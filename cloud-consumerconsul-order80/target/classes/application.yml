server:
  port: 80

spring:
  application:
    name: cloud-consumer-order
  cloud:
    consul:
      host: http://192.168.205.129
      port: 8500
      discovery:
        service-name: ${spring.application.name}